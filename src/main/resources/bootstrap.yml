
spring:
  application:
    name: cluster-api
  cloud:
    config:
      uri: http://cloud.configserver:8888/
      name: cluster-api
      enabled: true

endpoints:
  shutdown:
    #No auth  protected
    sensitive: false
    #Enable shutdown endpoint
    enabled: true
